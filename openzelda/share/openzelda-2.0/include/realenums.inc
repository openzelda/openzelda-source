#if defined realenum_included
	#endinput
#endif
#define realenum_included

const round_method: {
	round_normal = 0,
	round_floor,
	round_ceil,
	round_tozero,
	round_unbiased
}

const anglemode: {
	radian = 0,
	degrees,
	grades
}
